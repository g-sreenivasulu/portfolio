---
import TimeLineElement from "./timeline.astro";
import profile from "@data/profile.json" ;

const education = profile.education;
const experience = profile.experience;
const certificates = profile.certificates;
const subjects = profile.subjects;
---

<div>
    <div class="mb-5">
      <div class="text-3xl w-full font-bold">Profile</div>
    </div>
  
    <div class="mb-10 mx-4 text-left">
      I am Dr. Sreenivasulu Gogula, an accomplished Professor and Head of the
      Department of CSE (Artificial Intelligence & Machine Learning) at ACE
      Engineering College, Hyderabad. With a Doctoral degree from JNTUH and a
      focus on Deep learning and Natural Language Processing, I am a renowned
      author and researcher in the field of Machine Learning and Deep Learning. I
      have filed more than 10 National patents and been granted 3 International
      Australian Patents, demonstrating my commitment to innovation and
      excellence.
    </div>
  
    <div class="mb-5">
      <div class="text-3xl w-full font-bold">Education</div>
    </div>
  
    <div class="time-line-container grid gap-4 mb-10">
      {
        education.map((education) =>(
          <TimeLineElement
            title = {education.degree}
            subtitle={education.timespan}
          />
        ))
      }
    </div>
  
    <div class="mb-5">
      <div class="text-3xl w-full font-bold">Experience</div>
    </div>
  
    <div class="time-line-container grid gap-4 mb-10">
      {
        experience.map((experience) =>(
          <TimeLineElement
            title = {experience.college}
            subtitle={experience.timespan}
          />
        ))
      }
    </div>
  
    <div class="mb-5">
      <div class="text-3xl w-full font-bold">
        NPTEL & Coursera-AICTE Certifications
      </div>
    </div>
  
    <ul class="list-disc mx-6 mb-10 grid gap-2">
      {certificates.map((certificate) => <li>{certificate}</li> )}
    </ul>
  
    <div class="mb-5">
      <div class="text-3xl w-full font-bold">Subjects Taught</div>
    </div>
  
    <ul class="list-disc mx-6 mb-10 grid gap-2">
      {subjects.map((subject) => <li>{subject}</li> )}
    </ul>
  </div>
  